# Subarrays (`subarrays.java`)

## Description
Generates and prints all possible contiguous subarrays of a given array.

## How it Works
Uses three nested loops:
1. Outer loop (`i`) selects the starting point.
2. Middle loop (`j`) selects the ending point.
3. Inner loop (`k`) prints all elements from index `i` to `j`.
